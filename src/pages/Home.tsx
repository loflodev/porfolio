import type { FC } from 'react';

import Sidebar from '../components/layouts/Sidebar/Sidebar';

const Home: FC = () => {
  return (
    <main>
      <Sidebar />
    </main>
  );
};

export default Home;
