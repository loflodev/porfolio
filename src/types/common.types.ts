export type CardType = {
  title: string;
  image: string;
  alt: string;
  description: string;
  date?: string;
};

export type ClientSectionType = {
  to: string;
  image: string;
  alt: string;
};

export type TimelineType = {
  title: string;
  time: string;
  description: string;
};

export type SkillType = {
  title: string;
  progress: string;
  width: string;
};
